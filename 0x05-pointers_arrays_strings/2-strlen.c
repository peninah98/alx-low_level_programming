#include "main.h"

/**
 * _strlen - function that returns the length of astring
 * @s:Input string
 * Return:This function will return the length of string
 */
int _strlen(char *s)
{
	int index;
	for (index = 0; s[index] != '\0' ; index++)
		:
	return(index);
}
