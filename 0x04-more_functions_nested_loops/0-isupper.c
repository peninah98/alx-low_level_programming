#include "main.h"

/**
 * isupper - if a letter is an uppercase.
 *@c: print int
 * Return: Always 0.
 */
int _isupper(int c)
{
if(c>='A" && c<='z')
   {
       return(1);
   }
else
   {
       return(0);
   }
     
   
    
}
